import 'package:flutter/material.dart';

class DPayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('D-Pad 페이지'),
      ),
      body: Center(
        child: Text('D-Pad 페이지'),
      ),
    );
  }
}