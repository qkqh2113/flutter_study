import 'package:flutter/material.dart';

class DFoodPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('D-Food 페이지'),
      ),
      body: Center(
        child: Text('D-Food 페이지'),
      ),
    );
  }
}